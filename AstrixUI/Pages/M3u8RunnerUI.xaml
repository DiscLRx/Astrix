<Page x:Class="AstrixUI.Pages.M3u8RunnerUI"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:hc="https://handyorg.github.io/handycontrol"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:local="clr-namespace:AstrixUI.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="M3u8Runner">
    <Page.Resources>
        <local:FailedCountGreaterThan0Converter x:Key="FailedCountGreaterThan0Converter" />
    </Page.Resources>
    <StackPanel Orientation="Horizontal" Margin="65,5,0,0" >
        <StackPanel Orientation="Vertical"  Width="700">
            <DockPanel LastChildFill="True">
                <RadioButton DockPanel.Dock="Left" VerticalAlignment="Bottom" Margin="0,8,8,8" GroupName="m3u8Source" IsChecked="{Binding M3u8LinkIsChecked}" />
                <TextBox DockPanel.Dock="Left" Margin="0,2,0,0" Style="{StaticResource TextBoxExtend}" hc:InfoElement.Title="M3U8 Link" MaxLength="2000" Text="{Binding M3u8Link}" IsEnabled="{Binding M3u8LinkIsChecked}"/>
            </DockPanel>
            <DockPanel LastChildFill="True">
                <RadioButton DockPanel.Dock="Left" VerticalAlignment="Bottom" Margin="0,8,8,8" GroupName="m3u8Source" IsChecked="{Binding M3u8LocalFileIsChecked}"/>
                <TextBox DockPanel.Dock="Left" Margin="0,2,0,0" Style="{StaticResource TextBoxExtend}" hc:InfoElement.Title="M3U8 Local File" Width="605" MaxLength="2000" Text="{Binding InputFile}" IsEnabled="{Binding M3u8LocalFileIsChecked}"/>
                <Button DockPanel.Dock="Left" VerticalAlignment="Bottom" Width="60" Margin="10,0,0,0" Content="Select" HorizontalAlignment="Left" Foreground="#ccc" BorderBrush="#ccc" Command="{Binding SelectInputFileCmd}" IsEnabled="{Binding M3u8LocalFileIsChecked}"/>
            </DockPanel>
            <TextBox Margin="0,2,0,0" Style="{StaticResource TextBoxExtend}" hc:InfoElement.Title="Url Base" MaxLength="2000" Text="{Binding UrlBase}"/>
            <DockPanel LastChildFill="True">
                <TextBox DockPanel.Dock="Left" Margin="0,2,0,0" Style="{StaticResource TextBoxExtend}" hc:InfoElement.Title="Output Directory" Width="560" MaxLength="2000" Text="{Binding OutputDirectory}"/>
                <Button DockPanel.Dock="Left" VerticalAlignment="Bottom" Width="60" Margin="10,0,0,0" Content="Select" HorizontalAlignment="Left" Foreground="#ccc" BorderBrush="#ccc" Command="{Binding SelectOutputDirectoryCmd}"/>
                <Button DockPanel.Dock="Left" VerticalAlignment="Bottom" Width="60" Margin="10,0,0,0" Content="Open" HorizontalAlignment="Left" Foreground="#974" BorderBrush="#974" Command="{Binding OpenOutputDirectoryCmd}"/>
            </DockPanel>
            <DockPanel Margin="0,2,0,0" LastChildFill="True">
                <TextBlock DockPanel.Dock="Right" Text=".mp4" TextAlignment="Left" VerticalAlignment="Bottom" Margin="4,0,0,6" Foreground="#bbb" />
                <TextBox DockPanel.Dock="Right" Style="{StaticResource TextBoxExtend}" hc:InfoElement.Title="Video Name" MaxLength="2000" Text="{Binding VideoName}"/>
            </DockPanel>
            <TextBox Margin="0,2,0,0" Style="{StaticResource TextBoxExtend}" hc:InfoElement.Title="Tasks Count Limit" MaxLength="2000" Text="{Binding TasksCountLimit}"/>
            <StackPanel Orientation="Horizontal" Margin="0,15,0,0" VerticalAlignment="Top">
                <StackPanel Orientation="Vertical" Margin="0,0,20,0">
                    <Button Content="运行" Foreground="#27f" BorderBrush="#27f" Width="100" Margin="0,0,0,12" IsEnabled="{Binding IsRunButtonEnabled}" Command="{Binding Run}" />
                    <Button Content="重试" Foreground="#974" BorderBrush="#974" Width="100" Margin="0,0,0,12" Command="{Binding Retry}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource ButtonBaseStyle}">
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsRetryRunning}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="IsEnabled" Value="False" />
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding FailedCount, Converter={StaticResource FailedCountGreaterThan0Converter}}" Value="False" />
                                            <Condition Binding="{Binding IsRetryRunning}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="IsEnabled" Value="False" />
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding FailedCount, Converter={StaticResource FailedCountGreaterThan0Converter}}" Value="True" />
                                            <Condition Binding="{Binding IsRetryRunning}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="IsEnabled" Value="True" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button Content="临时目录" Command="{Binding ToggleTempDirectoryFlyoutOpen}" Width="100" Foreground="#aaa" BorderBrush="#aaa"/>
                    <ui:Flyout IsOpen="{Binding IsTempDirectoryFlyoutOpen}" Placement="Relative" Padding="0">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Closed">
                                <i:InvokeCommandAction Command="{Binding SetTempDirectoryFlyoutClosed}" ></i:InvokeCommandAction>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <Border CornerRadius="5" Width="100">
                            <StackPanel Orientation="Horizontal" hc:BorderElement.Circular="True" ClipToBounds="True">
                                <Button Content="打开" Command="{Binding OpenTemp}" Style="{StaticResource ButtonBaseStyle}" Width="47" hc:BorderElement.CornerRadius="5" Margin="2" Foreground="#aaa" BorderBrush="#aaa"/>
                                <Button Content="删除" Command="{Binding ClearTemp}" Style="{StaticResource ButtonBaseStyle}" Width="47" hc:BorderElement.CornerRadius="5" Margin="0,2,2,2" Foreground="#974" BorderBrush="#974"/>
                            </StackPanel>
                        </Border>
                    </ui:Flyout>
                </StackPanel>
                <StackPanel Orientation="Vertical" Width="70">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,5,0,0">
                        <TextBlock VerticalAlignment="Center" Foreground="#7C3" Text="Done: " />
                        <TextBlock VerticalAlignment="Center" Foreground="#7C3" Text="{Binding DoneCount}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,5,0,0">
                        <TextBlock VerticalAlignment="Center" Foreground="#f28" Text="Failed: " />
                        <TextBlock VerticalAlignment="Center" Foreground="#f28" Text="{Binding FailedCount}" />
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <Border CornerRadius="5" Margin="20,0,0,0" Height="165" Background="#222">
                        <ScrollViewer x:Name="logScrollViewer" CanContentScroll="True" VerticalScrollBarVisibility="Visible" MaxHeight="150" Padding="5">
                            <TextBlock Foreground="#aaa" Width="480" TextWrapping="Wrap" FontSize="10" Text="{Binding RuntimeMessage}" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </StackPanel>

</Page>

    